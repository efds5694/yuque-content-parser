{
  "type": "object",
  "properties": {
    "sourceHash": { "type": "string" },
    "summary": { "type": "string" },
    "operations": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "op": {
            "type": "string",
            "enum": ["replace_text", "insert_paragraph_after", "delete_paragraph"]
          },
          "blockId": { "type": "string" },
          "oldText": { "type": "string" },
          "newText": { "type": "string" },
          "reason": { "type": "string" },
          "factChange": { "type": "boolean" }
        },
        "required": ["op", "blockId", "oldText", "newText", "reason", "factChange"],
        "additionalProperties": false
      }
    },
    "warnings": {
      "type": "array",
      "items": { "type": "string" }
    }
  },
  "required": ["sourceHash", "summary", "operations", "warnings"],
  "additionalProperties": false
}
